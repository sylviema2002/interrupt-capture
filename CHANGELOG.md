# 中断快记版本更新记录

## 0.7.1

- 两个版本的“提交反馈”按钮接入 GitHub Issue 页面，会自动带上反馈模板。
- README 的飞书同步版 AI 配置提示词改为直接使用公开 GitHub 地址。
- 两个版本同步更新：飞书同步版和本地版。

## 0.7.0

- 移除“复制使用概况”，保留更有用的“提交反馈”。
- 单条事项“改时间”改为“稍后提醒”，入口更贴近日常使用。
- “稍后提醒”支持 15 分钟后、1 小时后、明天 9 点、自定义分钟后和自定义日期时间。
- 两个版本同步更新：飞书同步版和本地版。

## 0.6.9

- 调整“复制使用概况”的日期统计口径：改为按记录创建日期归类，并显示该日创建记录的当前状态。
- 飞书同步版的使用概况补充说明：只统计当前本地保留记录，完整历史以用户自己的飞书表格为准。

## 0.6.8

- 两个版本的设置中新增“提交反馈”按钮。
- 两个版本的设置中新增“复制使用概况”按钮，只复制数量概况，不包含事项内容、来源标题或来源链接。
- GitHub 仓库地址配置前，“提交反馈”会先复制反馈模板；配置后可直接打开 GitHub Issue 页面。

## 0.6.7

- 移除“自愿匿名统计”开关和本机统计计数逻辑，继续保持工具轻量。
- 保留隐私说明：插件默认不上传个人记录到作者或第三方服务器。
- 保留使用说明中的反馈入口，鼓励用户主动反馈问题和使用场景。
- 补充 `.gitignore` 安全项，避免误传 `sync-config.json`、`.sync-tmp/`、`*.pem` 和 `*.crx`。

## 0.6.6

- README 增加隐私说明：插件默认不上传个人记录到作者或第三方服务器。
- 使用说明增加反馈入口，方便使用者主动提交问题和使用场景。

## 0.6.5

- 仅更新本地版备份逻辑，飞书同步版不变。
- 本地版主界面恢复“复制未完成”，只复制当前未完成事项，便于发给别人或发给自己。
- 本地版设置中新增“导出全部数据”，用于低频备份全部记录。
- 本地版设置中新增“清理已完成”，会删除此刻之前所有已完成记录；下次导出全部数据时，从清理后保留和新增的数据开始导出。

## 0.6.4

- 本地版完成事项后不再直接删除，而是保留为已完成记录并记录完成时间。
- 本地版“复制/导出全部”会导出未完成、暂停、已完成的全部记录。
- 本地版待办列表仍只显示未完成和暂停事项，避免日常界面变重。

## 0.6.3

- 飞书同步版本地服务新增 `serviceToken` 鉴权，减少其他网页误写入本地同步服务的风险。
- 飞书同步版复制未完成事项后增加成功或失败反馈。
- 本地版提醒窗口移除“写入飞书失败”文案，改为通用操作失败提示。

## 0.6.2

- 移除浏览器右下角系统通知。
- 提醒时只保留屏幕中间的强提醒窗口。
- 移除 `notifications` 权限，减少浏览器权限提示。

## 0.6.1

- 提醒窗口改为屏幕居中打开。
- 提醒窗口会定时尝试回到前台，降低被其他软件盖住后错过提醒的概率。
- 新增 `system.display` 权限，仅用于计算屏幕居中位置。

## 0.6.0

- 新增右上角“设置”，可修改默认提醒时间。
- 新增单条事项“改时间”，可只调整这一条的提醒间隔。
- “再等 X 分钟”“恢复计时”和循环提醒会沿用该事项自己的提醒时间。
- 两个版本同步更新：飞书同步版和本地版。
- 已安装用户更新方式：替换插件文件后，到 Chrome 或 Edge 的扩展程序页面点击“重新加载”或“刷新”。

## 0.5.0

- 整理为开源项目结构。
- 提供飞书同步版和本地版两个安装包。
- 本地版支持复制未完成事项并导出 TXT。
- 飞书同步版支持未完成、已完成、暂停状态同步。
