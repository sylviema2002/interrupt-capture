# 中断快记版本更新记录

## 1.0.5

- 补充跨平台本机辅助程序雏形，便于后续在更多电脑环境中实机验证。
- 对外说明不承诺未实测环境的适配状态，统一建议由 AI 助手按当前电脑环境配置。
- 同步服务启动时会补充 Homebrew 常见路径，减少从 Finder 启动时找不到 `node` 或 `lark-cli` 的概率。
- 公开安装包继续只包含飞书同步版，不包含个人 `sync-config.json`。

## 1.0.4

- 没有时间词的普通文本停顿约 1.5 秒后，也会按默认分钟数自动创建中断任务，不再需要手动点击默认提醒按钮。
- 带时间任务继续保持约 0.8 秒自动创建。
- 修复“明天九点半汇报”这类半点表达未进入规划任务/规划日程的问题。
- 对外安装包更新为 `interrupt-capture-feishu-cli.zip`。

## 1.0.3

- 重新梳理中断恢复、长期规划和待安排三条任务路径。
- 新增“中断任务、规划任务、待安排”三个分类按钮和数量显示。
- 分类改为按日期判断：未明确跨日日期时归中断任务；明天、后天或具体日期归规划任务。
- 未填写时间的文本按默认分钟数创建中断任务；待安排只接受从前两类手动转入。
- 支持自然语言时间点和时间段，并兼容任务在时间前后两种语序。
- 修复语音分段输入时在“10 点到”处提前提交的问题；只有结束时间和任务内容完整后才自动写入。
- 规划任务自动创建飞书日程，并在开始时间弹出置顶强提醒。
- 飞书多维表格新增“任务类型”和日程时间字段；字段缺失时自动创建并重试。
- Windows 本机辅助程序保持提醒窗口置顶，并在后台托管飞书同步服务。
- 修复删除已不存在的飞书日程时报 `193003 event is deleted`、导致本地任务无法删除的问题。

## 0.8.6

- 右上角设置里的默认提醒时间改回手填，避免长下拉列表打断轻量体验。
- “稍后提醒”的自定义分钟改回手填；自定义日期时间保留系统日期和时间控件。
- “加到日程 / 更改日程”改为日期、开始时间和结束时间，不再单独选择时长。
- 收窄提醒窗口里的选择弹层，避免稍后提醒选项被窗口边缘遮挡。
- 清理上一轮滚轮方案遗留的废弃代码和文档表述。

## 0.8.5

- “稍后提醒”自定义分钟、自定义日期时间曾改为滚轮选择，后续根据实际体验调整回更轻的输入方式。
- “加到日程 / 更改日程”支持日期、小时、分钟、日程时长选择。
- 右上角设置里的默认提醒时间支持常用时间选择。

## 0.8.4

- 新增轻量日程校验：打开插件时，会检查已加入的飞书日程是否仍存在。
- 如果用户在飞书日程里手动删除了由中断快记创建的日程，插件会提示“飞书日程已被删除，可重新加入日程”。
- 日程被手动删除后，飞书表格仍保留事项状态为“暂停”，并新增或更新“日程状态”为“已删除”。
- 重新加入日程后，“日程状态”会恢复为“已加入”。

## 0.8.0 - 0.8.3

- 优化飞书日程标题和详情格式。
- 优化本地同步服务调用方式，减少 Windows 多行文本导致参数丢失的问题。
- 主界面、提醒弹窗和时间选择逐步调整为更接近飞书日程的视觉风格。
- 支持删除事项时同步删除已关联的飞书日程，并把表格状态改为“已删除”。

## 0.7.x

- 接入 GitHub Issue 反馈入口。
- 单条事项“改时间”改为“稍后提醒”。
- 修复 Windows 下 Node.js 已安装但不在 PATH 时，双击 `start-sync-service.cmd` 一闪而过的问题。

## 0.6.x

- 增加默认提醒时间、暂停、恢复、复制未完成等能力。
- 增加本机同步服务 `serviceToken` 鉴权。
- 移除浏览器右下角系统通知，只保留屏幕中间强提醒窗口。

## 0.5.0

- 整理为开源项目结构。
- 支持未完成、已完成、暂停状态同步到飞书多维表格。
