# 桌面版设计草案

桌面版不是替代浏览器插件，而是中断快记的另一个入口。它负责接住 Excel、Word、PPT、WPS、微信、飞书客户端、PDF 等非浏览器场景，数据仍写入同一套飞书多维表格和飞书日程。

## 交互原则

- 快捷键唤起快记小窗。
- 唤起瞬间先记录当前前台工作窗口，再显示快记小窗。
- 自动排除中断快记自己的窗口，避免把快记小窗或提醒窗识别成来源。
- 用户可以点击“点选来源”，再点击另一个已打开窗口，把来源替换成那个窗口。
- 输入任务后按 Enter 提交；Enter 不作为换行。
- 语音输入依赖系统或输入法自带能力，工具只接收转写后的文字。

## 来源识别

桌面版来源按分层策略保存：

- 通用窗口信息：应用名、进程名、窗口标题、窗口句柄。
- 文件线索：文件路径、文件大小、修改时间、可用时保存文件稳定标识。
- 专属增强：浏览器 URL、Office/WPS 文件路径、PDF 文件路径等。
- 兜底信息：用户看到的来源文本。

提醒时点击“回到现场”：

1. 原窗口还在，优先切回原窗口。
2. 原文件路径还在，打开原文件。
3. 文件稳定标识或相似文件能找回，提示用户确认后打开。
4. 都找不到时，展示来源文本。

## 任务判断

桌面版复用浏览器插件的时间判断原则：

- 没有跨天日期：中断任务，按默认间隔或指定当日时间强提醒。
- 明确“明天”“后天”或具体日期：规划任务，写入飞书日程。
- 暂时不需要安排：进入待安排。

## 本地数据

本地只保留未完成、规划中、待安排事项和必要找回线索。用户点击完成或删除后：

- 先写入飞书多维表格，更新状态和完成/删除时间。
- 如有关联飞书日程，同步删除或更新日程状态。
- 成功后从本地删除该事项。

长期留痕放在飞书，本机只做短期提醒和“回到现场”。
